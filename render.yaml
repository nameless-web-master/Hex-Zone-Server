services:
  - type: web
    name: fastapi-app
    env: docker
    plan: free
    region: oregon
    branch: main
    healthCheckPath: /
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: fastapi-db
          property: connectionString
databases:
  - name: fastapi-db
    plan: free