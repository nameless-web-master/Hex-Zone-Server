services:
  # PostgreSQL with PostGIS
  postgres:
    image: postgis/postgis:16-3.5
    environment:
      POSTGRES_USER: zoneweaver
      POSTGRES_PASSWORD: zoneweaver_pass
      POSTGRES_DB: zoneweaver
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./init_db.sql:/docker-entrypoint-initdb.d/init.sql
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U zoneweaver"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - zoneweaver

  # FastAPI Backend
  api:
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      DATABASE_URL: postgresql://postgres:NJptCdSyqVTEfxlwhUNihapUusGrYFGQ@maglev.proxy.rlwy.net:14968/railway
      SECRET_KEY: your-change-me-secret-key-minimum-32-characters-long
      ALGORITHM: HS256
      ACCESS_TOKEN_EXPIRE_MINUTES: 30
      H3_DEFAULT_RESOLUTION: 13
      MAX_ZONES_PER_USER: 3
    ports:
      - "8000:8000"
    depends_on:
      postgres:
        condition: service_healthy
    volumes:
      - ./app:/app/app
    networks:
      - zoneweaver
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload

  # pgAdmin for GUI database management
  pgadmin:
    image: dpage/pgadmin4
    environment:
      PGADMIN_DEFAULT_EMAIL: admin@admin.com
      PGADMIN_DEFAULT_PASSWORD: admin
    ports:
      - "8080:80"
    depends_on:
      - postgres
    networks:
      - zoneweaver

volumes:
  postgres_data:

networks:
  zoneweaver:
    driver: bridge